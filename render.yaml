services:
  - type: web
    name: poke-collect-backend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    nodeVersion: 18.17.0
    envVars:
      - key: NODE_ENV
        value: production
